<h1><%= @expensesSheet.name %>#fullcalendar</h1>

<div id='calendar'></div>

<script>
  $('#calendar').fullCalendar({
    firstDay: 1,
    events: <%= raw @expenses_calendar.to_json() %>
  });
</script>